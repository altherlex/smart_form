module SmartFormHelper
  def mount_form_fields(params={})
    return "" if params.empty?
    #params = params.with_indifferent_access
    #case params[:]
    #when 'links_rapidos', 'itens_recentes', 'notas_versao', 'estilos';
    #else
    #end

  end
end
