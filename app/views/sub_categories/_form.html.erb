<%= form_for(@sub_category) do |f| %>
  <% if @sub_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sub_category.errors.count, "error") %> prohibited this sub_category from being saved:</h2>

      <ul>
      <% @sub_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :category %><br>
    <%= f.collection_select(:category_id, Category.all, :id, :name, {:include_blank => true, :require=>'require'}) %>
  </div>
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :slug %><br>
    <%= f.text_field :slug %>
  </div>
  <div class="field">
    <%= f.label :fields %><br>
    <%=text_field_tag :order, nil, type: :text, placeholder: "order", required:'required'%>
    <%=text_field_tag :title, nil, type: :text, placeholder: "title", required:'required'%>
    <%=text_field_tag :type, nil, type: :text, placeholder: "type", required:'required'%>
    <%=text_field_tag :values, nil, type: :text, placeholder: "values"%>
    <button id="add-btn" style="display: inline-block;">Add</button>
    <button id="edit-btn" style="display: none;">Edit</button>
    <button id="add-btn" style="display: none;">Cancel</button>
    <%#= f.text_area :fields %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
